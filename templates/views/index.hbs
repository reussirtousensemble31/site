{{!< default}}

<script>
    $(function() {
        // summary:
        //      Adjust this section to window height
        var h = $(window).height() - $('.navbar').height() - 90;

        $('#imageTitle').height(h);
    });
</script>

<section id="imageTitle">
    <div class="container">
        <div class="row">
            <div class="col-sm-10 col-sm-offset-1">
                <h1>Partageons ensemble dans le sport !</h1>
            </div>
        </div>
    </div>
</section>

<section class="section" id="menu">
    <div class="container">
        <div class="row">
            {{# each data.sections}}
                <a href="#{{ancre}}">
                <div class="col-sm-4 text-center">
                    <div class="rounded-topic section-color-green">
                        <i class="fa {{nomIcone}} fa-5x"></i>
                        <div class="title">{{titre}}</div>
                    </div>
                </div>
                </a>
            {{/each}}
            <div class="col-sm-4 text-center">
                <div class="rounded-topic section-color-green">
                    <i class="fa fa-calendar fa-5x"></i>
                    <div class="title">Planning</div>
                </div>
            </div>
        </div>
    </div>
</section>

{{# each data.sections}}
    <section id="{{ancre}}" class="{{#if alt}}section-alt{{else}}section{{/if}} ">
        <div class="container">
            <div class="row">
                <h2>
                    {{titre}}
                </h2>
                <div>
                    {{{contenu}}}
                </div>
            </div>
        </div>
    </section>
{{/each}}

{{>map}}

{{>contact}}